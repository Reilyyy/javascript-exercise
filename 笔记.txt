数组：
按照下标查找元素很方便，可以实现修改
但是扩容和中间插入以及删除操作性能比较低
js数组就是api的调用

forEach函数：遍历数组中每个元素并执行回调函数
特点：返回值是undefined 回调函数return不能终止循环 不会改变原数组
未赋值的索引不会被遍历到 
执行过程中添加元素、修改元素 遍历结果不会改变，回调函数在第一次执行时就已经确定了
执行过程中删除已经遍历过得元素值，则后面的元素会被跳过。